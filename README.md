# bcBlog

一个前后端分离的个人博客项目，前台面向游客开放阅读，后台供管理员发布内容、管理站点。支持二次元风格主题、多种氛围组件，并逐步向论坛方向扩展。

## 技术栈

- 后端：Spring Boot 2.6.13 / Java 8 / MyBatis-Plus / Sa-Token / MySQL 8
- 前端：Vue 3 / Vite / Element Plus / Pinia / Axios / wangEditor

## 现有功能

### 前台

- 首页文章卡片瀑布流 + 时间线布局
- 首页中段最新文章轮播、音乐播放器与歌词栏
- 文章详情、归档、搜索，文章详情展示封面
- Gitalk 评论（基于 GitHub Issues），首页最近评论下拉展示
- 流光忆庭照片墙、智库资源区
- 白天樱粉 / 夜晚星空主题，可自由切换
- 樱花、星空粒子、点击涟漪等氛围组件
- 网易云音乐播放器、一言、Live2D 看板娘
- 实时天气卡片（自动定位 + 北京时间）
- 站点公告栏、阅读进度、图片灯箱、代码高亮与复制、回到顶部
- 页脚显示系统稳定运行时间与北京时间
- 用户注册 / 登录（邮箱即账号）、邮箱验证码、用户中心
- 用户等级与经验、积分、每日签到、邀请码
- 原生评论系统（可后台切换到 Gitalk），未登录可看、登录后可评论
- 评论与文章编辑支持表情包
- 智库资源详情页，前往资源消耗积分，解锁后不重复扣除
- 沙盒世界：AI 角色在地图上的自动生活，含地图、档案、状态与行动时间线
- 沙盒旅人低语：登录用户消耗积分给角色留言，角色下次行动会参考
- 沙盒金币：登录用户可用积分为角色贡献金币，角色会在日常活动中赚取或花掉

### 后台

- 管理员登录（图形验证码、失败锁定、IP 频率限制）
- 仪表盘：文章数据、访问量统计、系统性能监控、系统信息与依赖版本
- 文章 / 分类 / 标签 / 评论管理
- 富文本文章发布与编辑，本地图片上传
- AI 一键写文章，支持多个 AI 服务商切换
- 分类 + 标签管理，支持多级分类
- 站点设置（名称、Logo、备案、SEO、首页标语）
- 背景壁纸管理（图片 / 视频，前后台可独立设置，后台背景透明度可调）
- 看板娘模型管理、网易云歌单管理、站点公告管理
- 流光忆庭照片管理、智库资源管理
- 管理员分级（超级管理员 / 一级 / 二级）与一二级菜单授权
- 登录日志查看、清空与 IP 位置查询
- 第三方接口配置（天气、一言、敏感词、IP 定位、ACG 随机封面、Gitalk 评论）
- 用户管理：普通用户列表、封禁、积分发放（单个或全站）
- 等级配置、邀请码管理、表情包管理
- QQ 邮箱配置与邮件模板管理（多套模板切换、预览、测试发送）
- 评论系统切换（Gitalk / 原生评论）、Gitalk 评论后台管理
- 数据清理：按天自动清理登录日志、访问统计、签到与积分流水，执行时间与保留天数可配
- 沙盒世界管理：地图背景、地名与坐标（可拖动标记）、世界观设定、运行参数（开关 / 间隔 / 夜间静默 / 每日上限 / 低语积分）
- 沙盒角色管理：人设、立绘、绑定任意 AI 服务商与模型、启停自动行动、一键立即执行
- 沙盒日志：行动记录与旅人低语查看、删除

## 目录结构

```text
bcBlog/
├── src/main/java/com/bc/bcblog/   # 后端代码
├── src/main/resources/            # 配置文件
├── frontend/                      # Vue3 前端
├── docs/sql/                      # 数据库脚本（全量安装 / 升级脚本 / 服务器结构快照）
└── tools/                         # 辅助工具（数据库结构对比、密码重置）
```

## 快速开始

1. 准备数据库（详见 `docs/sql/README.md`）：
   - 全新安装：执行 `docs/sql/bc_blog_full.sql`，创建 `bc_blog` 库中的全部 37 张表。
   - 已有旧库升级：执行 `docs/sql/upgrade_20260916_batch.sql`（已合并 001~033 全部改动），脚本幂等、可重复执行，不会清空数据。
2. 修改 `src/main/resources/application.yml` 中的数据库账号密码。
3. 启动后端：运行 `BcBlogApplication`。
4. 启动前端：

```bash
cd frontend
npm install
npm run dev
```

5. 访问前台 http://localhost:5173，后台 http://localhost:5173/login。

首次启动后端会自动创建默认管理员：

- 用户名：`admin`
- 密码：`Admin@123456`

登录后请尽快在后台右上角修改密码。

## 更新日志

### 2026-09-16

- 修复沙盒「输出自查」丢失字段的问题：自查提示词补全全部字段清单并强调原样保留，服务端新增校验（自查结果一旦丢了原 JSON 的字段就整份丢弃）
- 自查并入行动重试循环，先自查、再校验字段，避免自查结果直接落库导致所有角色都显示兜底值「稍作停留」
- 角色行动提示词要求 `next_after_minutes` / `next_after_reason` 必填，缺失时自动重试并在提示词中点名缺失字段
- 沙盒每日记忆由流水账改为第一人称故事化叙述，支持在后台按指定日期补生成 / 覆盖记忆
- 新增配置 `sandbox_system_model`：定时任务等系统级 AI 调用可单独指定模型
- **合并数据库升级脚本**：新增 `docs/sql/upgrade_20260916_batch.sql`，一份脚本涵盖 001~033 全部表 / 字段 / 索引 / 配置变更，旧的 `upgrade_20260915_batch.sql` 已并入并删除
- 重新导出 `docs/sql/bc_blog_full.sql`（37 张表，仅结构，不含数据），并重写 `docs/sql/README.md`（执行方式、改动清单、表用途与清理范围）
- 新增**数据库结构对比工具** `tools/schema-diff/schema-diff.ps1`：上线前把服务器表结构导出到 `docs/sql/schema/`，即可自动比对出「服务器缺哪些表 / 字段 / 索引」，并可生成幂等的升级 SQL 草稿（配套说明见 `tools/schema-diff/README.md`）

### 2026-09-15

- 新增「沙盒世界」模块：AI 角色在地图上自动生活，前台可看地图、档案、状态与行动时间线
- 沙盒支持自定义地图背景、地点与坐标，管理员可拖动地图标记直接改坐标
- 沙盒角色支持自定义人设（写法参考酒馆角色卡）、立绘、绑定任意 AI 服务商与模型
- 沙盒自动行动：按随机间隔（默认 45~75 分钟）调用 AI，支持夜间静默、每日上限、总开关与「立即执行一次」
- 新增旅人低语互动：登录后消耗积分给角色留言，角色下一次行动会把低语作为参考；管理员留言不扣积分
- 新增数据库脚本 `docs/sql/upgrade_017_sandbox.sql`，并重新导出 `docs/sql/bc_blog_full.sql`（29 张表）
- 沙盒地点支持内置图标（图钉/村庄/森林/魔法塔/湖泊/遗迹/酒馆/集市/山脉/据点/传送门）与自定义图标上传
- 地点编辑改为地图下方的内联表单，编辑时地图上实时显示预览标记，可直接拖动确认落点
- 前台沙盒角色档案支持搜索与折叠，旅人低语可以指定互动角色
- 沙盒角色新增金币：AI 会在行动中赚取或消耗金币，前台用户可用积分贡献金币（默认 1 积分 = 10 金币，后台可调）
- 沙盒提示词改为携带完整时间（日期、星期、具体时刻、时段、北京时间），并要求 AI 的行为符合当前时段
- 后台沙盒角色支持直接修改体力、魔力、饥饿度、心情、金币，并可为角色添加自定义状态项
- 新增金币流水（后台「沙盒日志 → 金币流水」可查可删），行动记录会标注每次金币变化
- 前台角色档案显示「下次行动」时间与倒计时（停用或沙盒暂停时会说明原因）
- 多角色同时行动：已到期或即将到期的角色合并成同一轮一起行动，角色能看到同世界其他居民及其最近动向
- 沙盒角色之间可以互动：AI 返回互动的角色名后，前台时间线与后台日志会显示「与 XX 互动」标签
- 后台沙盒角色页新增「全员行动一轮」按钮，便于一次推进所有角色观察互动
- 新增角色好感度：AI 会根据互动自动加减（-100~100，分 敌视 / 反感 / 陌生 / 相识 / 友好 / 亲近 / 挚友），前台角色档案展示双方对彼此的好感度
- 后台「沙盒日志 → 角色关系」可查看并手动修正任意两位角色之间的好感度与备注
- 行动时间线会显示「好感 XX +3」这类变化标签
- 修正好感度结算：行动记录只记录**实际生效**的变化（到达 ±100 被截断时不再显示虚假增加），并显示变化后的数值
- 角色关系展示「较上次 +1」标记，小幅度变化也能一眼看出来
- 新增可选的 AI 输出自查开关 `sandbox_verify_enabled`：开启后每次行动额外调用一次 AI 只做 JSON 校验与数值修正（默认关闭，会翻倍消耗 token）
- 新增「互动回应回合」：角色行动时如果与其他角色产生互动，对方会立即行动一次，避免两边信息不对等
- 回应链有四层防循环：深度上限（默认只回应一轮）、每轮回应次数上限、回应冷却时间、本轮已排队角色不重复触发；行动记录与时间线会用「回应」标签区分
- 新增每日记忆总结：每天到点（默认 23:50）把角色当天的行动整理成一段第一人称长期记忆，后续几天的提示词会携带最近 5 天记忆；AI 总结失败时自动用行动概括兜底拼接
- 新增角色背包：AI 行动时会在提示词里强调背包内容，并通过 items_change 增减物品（没有的东西不能减少、单次变化 ≤9、最多 20 种），前台档案展示背包与「最近的记忆」
- 日志与记忆保留天数接入「系统设置 → 数据清理」：沙盒行动日志默认 7 天、记忆默认 30 天；另有「总结后立即删当天日志」开关（默认关闭）
- 后台「沙盒日志 → 每日记忆」可查看/编辑/删除记忆，也可一键立即生成当天记忆；「沙盒角色 → 背包」可手动增删改物品
- 前台背包改为二次元游戏风格的道具格：按品质（普通/精良/稀有/史诗/传说）显示不同颜色边框与光晕，格子右下角显示数量，点击格子查看物品详情
- 物品未上传图标时按物品名自动匹配 emoji 图标（面包/草药/戒指/地图/萤石灯等 40 余种关键词），后台可为物品上传自定义图标并调整品质
- AI 新获得的物品会按名字关键词推断初始品质（例如「神器」→传说、「戒指/宝石」→稀有、「草药」→精良），管理员可随时改
- 新增二级地点：角色每次行动会让 AI 自行创作一个具体的小地方（如「自由城邦联盟 · 东侧集市」），写进行动记录与当前位置，并进入每日记忆和其他角色看到的「最近的动静」；二级地点不占地图坐标
- 前台时间线新增按地点筛选；后台沙盒日志的地点列会同时显示一级与二级地点，也支持按地点筛选
- 后台「沙盒角色 → 新增角色」支持 **AI 一键创作**：可选服务商/模型、输入一句需求，AI 会结合世界观、地图地点与已有角色生成角色卡（名称、称号、外貌、人设、初始地点与二级地点、初始状态、金币、2~4 件初始物品）并填入表单，改完再保存
- 新增角色时可勾选「保存后立即生成第一条行动」（默认勾选），AI 与手动创建的角色都能在创建后立刻拥有开局剧情；AI 生成的初始物品会同时放进背包
- 沙盒角色的 AI 行动回复不再做敏感词过滤（原文入库），避免外部检测接口把「下身 / 中共 / 蕾丝 / 肉香」这类正常词替换成星号；前台用户输入（旅人低语、博客评论）仍按原逻辑过滤
- 前台地图把「同一地点」的角色合并成一个叠图簇（最多叠 3 个头像 + 人数徽标），点开弹出该地点的角色列表再选人；簇带同组光环、成员头像 40px（手机 34px）、整簇平滑移动，不再出现"错开后像站在不同地点"的观感问题
- 后台地图仍保留原来的标记错开（方便一个个拖动维护地点坐标），不受影响
- 地点由「一个坐标点」升级为「一块矩形区域」：后台可拖动整体移动、拖右下角手柄缩放，也可直接填左上角/右下角坐标；已有地点以原坐标为中心自动生成 12×7 的默认区域
- 角色归属改为按区域判定：坐标落在哪块区域内就属于该地点，不在任何区域内时算最近的地点；AI 每次行动都会被告知各区域范围，服务端还会把坐标夹进所选区域
- 前台地图默认**只显示地点区域**（半透明色块 + 虚线边框 + 图标与地名 + 人数徽标），**点击地点后**才在该区域下方展开这一地的角色头像（超过一行自动换行），不再出现头像盖住地名的情况；同一时间只展开一个地点
- 原先的「叠图簇」方案已被区域模型取代，相关前端布局工具一并移除
- 下次行动间隔改由 **AI 自己决定**：每次行动时额外输出 `next_after_minutes`（间隔分钟）与 `next_after_reason`（原因，如「睡觉」），服务端据此排期并夹在 15~720 分钟之间；AI 没给间隔时回退到角色原有的随机区间（45~75 分钟）
- 夜间静默**默认关闭**（起止时间相同即视为关闭）：角色会自己安排睡觉，不再需要额外的静默时段；如需启用，在后台把两个时间改成不同值
- 间隔上下限支持全局配置（沙盒世界 → 运行参数）与**按角色覆盖**（角色编辑里的「AI 间隔上下限」，留空用全局）
- 前台「下次行动」会显示原因，如「正在睡觉，约 6 小时后（07:00）」；后台沙盒日志新增「下次间隔」列，显示这一步之后预计多久再行动及原因
- 新增「旅人纪闻」：由 AI **独立生成**当天世界的重大事件（节庆、天象、商队、遗迹异动、灾害等，与世界本身有关而非围绕某个角色），前台在地图左上角以悬浮卡片展示，只显示当天、第二天自动清理
- 纪闻生成接口可在后台配置（服务商 / 模型 / 每次生成条数 / 附加要求），支持手动一键生成；也可以后台手动新增、编辑、置顶、停用、删除
- 角色每次行动都会在提示词里看到【今日要闻】以及**事件与自己的距离描述**（同一个地区 / 半天路程 / 一两天路程 / 非常遥远），并结合体力、魔力、金币与正在做的事自行决定是否关注或前往，也可以完全不理会
- 前台纪闻卡片手机端默认收起，点击条目会在地图上展开事件发生地
- 旅人纪闻支持**每天定时自动生成**（默认 07:00，可在栏目设置里改时间或关闭），同时保留手动生成
- 前台点击纪闻条目时，事件发生地会在地图上**闪烁高亮**；后台列表可直接一键切换**置顶**与**前台显示**
- 行动记录新增「纪闻」标记：AI 在 `news_refs` 里列出这一步听说/关注的纪闻（服务端只采纳当天真实存在的标题），前台时间线显示「听闻 · xxx」，后台沙盒日志有「纪闻」列
- 提示词新增**背包管理**要求：每次行动都要顺手判断能否使用或清理背包物品（吃的吃掉、没用的丢掉或送人，数量减到 0 自动移除），背包达到 12 种时会提示"比较满了"，防止物品无限堆积
- **管理端安全加固**：AI 服务商区分「系统服务商」（超管维护、定时任务使用）与「管理员自己的服务商」（手动调用使用自己的 Key，没配置会直接提示去配置，不会偷偷用超管的）
- 定时任务只使用系统服务商；角色的服务商属于某个管理员时，定时执行会自动回落到系统服务商并记录日志
- GitHub Token、QQ 邮箱授权码、ACG Token、百度/高德 Key、邀请码管理、管理员/用户管理 → **仅超级管理员可调用**（后端统一拦截，越权尝试会记入审计）
- 密钥**加密存储**：AES-256-GCM，主密钥放在与 jar 同级的 `bcblog-secret.key`（已加入 .gitignore，请自行备份）；后台接口只回显掩码（如 `sk-3****fac2`），表单留空表示不修改
- 新增**API 调用审计**：记录谁在什么时候调用了会消耗额度或涉及密钥的功能（来源、服务商、成败、耗时、IP），后台「系统安全 → API 调用审计」可查看并支持按管理员汇总；保留天数接入数据清理，**默认 3 天**
- 合并数据库改动：新增 `docs/sql/upgrade_20260915_batch.sql`，一条脚本涵盖上次部署到现在的全部表 / 字段 / 配置变更
- 新增 `docs/sql/README.md`：说明脚本执行顺序、本次改动清单、各表用途与定期清理范围
- 重新导出 `docs/sql/bc_blog_full.sql`（24 张表，仅结构，不含数据）
- 注册验证码邮件模板 HTML 单独存档到 `docs/email/register_code.html`
- 补充 README 前台 / 后台功能清单，并整理本次更新日志

### 2026-09-14

- 新增 QQ 邮箱 SMTP 邮件服务，邮件背景、透明度、正文模板可在后台编辑
- 邮件模板支持同一场景保存多套，后台下拉筛选并随时切换启用模板
- 注册邮箱验证码支持真实发送，未配置邮件服务时自动回退为测试验证码
- 新增前台用户注册/登录、用户中心、头像上传、每日签到
- 普通用户注册与登录改为邮箱账号，管理员仍使用用户名登录
- 注册密码要求字母+数字并二次确认，邮箱验证码改为字母+数字组合
- 新增等级与经验机制，等级名称和升级经验可在后台配置
- 新增普通用户积分机制：签到随机 1~3 积分，每天前三次评论各 +1 积分
- 后台支持给指定用户或全部普通用户发放积分，来源和说明可自定义
- 智库资源支持封面、详情页和积分设置，前往资源消耗积分，解锁后不重复扣除
- 管理员访问智库资源免积分；新增历史日志定期清理，保留天数后台可配
- 数据清理支持配置执行时间和各表保留天数，默认登录日志 7 天、访问统计/签到/积分流水 30 天，并带表说明和误清提醒
- 新增表情包管理，评论和文章编辑可插入管理员上传的表情
- 新增邀请码机制，超级管理员可分配邀请码权限，注册是否强制邀请码可配置
- 评论系统支持 Gitalk / 原生评论后台切换，原生评论需登录后发表
- 后台新增用户管理、等级配置、邀请码管理
- 加固后台权限，普通用户禁止访问 `/api/admin/**`
- 新增后台 Gitalk 评论管理：列表、搜索、回复、删除
- 新增歌单加载失败时的默认歌曲后台管理
- 重做后台登录页，采用与前台一致的玻璃拟态风格
- 优化前台文章布局，桌面端调整为三列并加宽主容器
- 新增完整部署文件：完整建表脚本、生产配置、Nginx 配置、systemd 服务与部署文档
- 新增宝塔 Linux 面板（OpenCloudOS 9）部署指南与 beicheng.cloud 站点配置示例
- 后台仪表盘增加访问量统计、系统性能监控、系统信息与依赖版本信息
- 前台新增流光忆庭照片墙与智库资源区，支持后台上传管理
- 评论系统替换为 Gitalk（GitHub Issues），首页增加最近评论下拉展示
- 首页新增最新文章轮播、音乐播放器与歌词栏
- 文章详情支持显示封面图
- 页脚增加系统稳定运行时间与北京时间
- 后台菜单重构为一级菜单 + 二级菜单，超级管理员可分级授权
- 新增登录日志 IP 位置手动查询
- 新增后台背景壁纸透明度调整
- 接入硅基流动 DeepSeek，AI 写文章默认使用并支持切换其他接口
- 新增 ACG 随机封面接口，新增文章时可一键获取随机封面
- 前台天气卡片增加实时北京时间显示

### 2026-09-13

- 完成前台氛围组件、管理端权限与 API 统一管理
- 完成管理端收尾：仪表盘统计、系统设置、登录日志、DeepSeek 余额查询
- 完善前台展示与游客评论、敏感词过滤
- 完成博客前后端基础框架：登录、分类、标签、文章、图片上传、密码重置工具
